import { Outlet } from "react-router-dom"
import IntroPage from "./IntroPage"

const Root = () => {
  return (
    <>
      {/* <Outlet /> */}
      <IntroPage/>
    </>
  );
};

export default Root;
